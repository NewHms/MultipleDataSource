<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.test.datasource.doubleSource.user.dao.User1Dao">
    <select id="getUserInfo1" resultType="com.test.datasource.doubleSource.user.bean.User">
        select id,
        name,
        sex,
        age
        from users
    </select>

    <select id="getUserInfo2" resultType="com.test.datasource.doubleSource.user.bean.User">
        select id,
        name,
        sex,
        age
        from users
    </select>
</mapper>